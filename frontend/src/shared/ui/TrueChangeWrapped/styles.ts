import styled from "styled-components";

export const TrueChangeWrapped = styled.div<{ active?: boolean }>`
  // background-color: ${({ active }) => (active ? "white" : "black")};
  // scale: ${({ active }) => (active ? "2" : "1")};
  // transform: initial 0.2;
  //display: flex;
  //justify-content: center;
  //align-items: center;
  //flex-direction: column;
`;
